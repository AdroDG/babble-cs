namespace ChattyCathy
{
    /// <summary>
    /// Node class
    /// Manages:
    /// - node configuration (heartbeat, store type ...)
    /// - transport layer (way to communicate between peers)
    /// - proxy layer (way to inject transactions and retrieve them in blocks)
    /// - node control layer (node's API)
    /// - logger
    /// - peer selection (from a list)
    /// - store layer (hg events, blocks, round time)
    /// - Timer factory
    /// </summary>
    public class Node
    {
    }
}
